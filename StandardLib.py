import numpy as np
import matplotlib.pyplot as plt

x = np.linspace(0,20,100) # Create a list of evenly spaced numbers from 0 to 100 with 20 margin.
plt.plot(x,np.sin(x))
plt.show() # This creates a sine Graph.plt


